{
  "name": "one-frontend-framework",
  "private": true,
  "workspaces": [
    "packages/core"
  ],
  "scripts": {
    "build": "npm run build --workspace=@one-framework/core",
    "lint": "npm run lint --workspace=@one-framework/core",
    "test": "npm run test --workspace=@one-framework/core"
  }
}